How to run The Virtual Machine: 

$ make

$ pcc Usage: %s [-l Tokens] [-a Assembly Code] [-v stack & cpu trace]

The program will request the name of the text file (the text file must be in the same folder and the name cannot be greater than 100 chars). 

(..select the input file: input.txt..)

